# Inventory Tracker

Simple inventory tracker — Flask backend + React frontend.

## Project structure
- `backend/` — Flask app (app.py)
- `frontend/` — React app
- `inventory.db` — SQLite DB (not committed)

## Run locally

### Backend
```bash
cd ~/InventoryTracker
source venv/bin/activate
cd backend
python3 app.py
